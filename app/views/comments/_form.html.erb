

<%#why does :comment work?%>

<%= form_for(:comment, :url => astronomical_item_comments_path(@astronomical_item.id)) do |f|  %> 

    
    <%= f.text_area :content, rows: 3, cols: 30, :required => true, :class => "comment_field" %> 
    <%# <%= f.text_area(:comment, :text, size: "20x30") %> 
    <%= f.hidden_field :user_id, value: @current_user.id %>

    <%= f.hidden_field :astronomical_item_id, value: @astronomical_item.id %>
    <br>
    <br>
    <%= f.submit :class => "btn btn-primary btn-sm" %>

<% end %>